4chan Monitor — v1 (feed, search, settings, live auto-refresh).

Install:
1. Download `fourchan-monitor.apk` below (on your phone).
2. Tap it to install — allow "install from unknown sources" if asked.
3. Open the app → Settings → paste your API token → Save & connect.

Push notifications arrive in v2 (after Firebase setup).
